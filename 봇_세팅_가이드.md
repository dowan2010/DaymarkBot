# 뉴로우 봇 — 다른 사람 컴퓨터에 세팅하기

## 전체 흐름

```
상대방 컴퓨터에서 repo 생성
→ 나를 Collaborator로 초대
→ 내가 코드 push
→ 상대방 컴퓨터에서 clone + 봇 실행
→ 이후 자동으로 변경사항 감지 & 재시작
```

---

## PART 1. 상대방 컴퓨터에서 할 일

### 1. Homebrew 설치 확인

```bash
brew --version
```

없으면 설치:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

---

### 2. gh CLI 설치

```bash
brew install gh
```

---

### 3. GitHub 로그인

```bash
gh auth login
```

- `GitHub.com` 선택
- `HTTPS` 선택
- `Login with a web browser` 선택 → 브라우저에서 로그인

---

### 4. 빈 repo 만들기

```bash
gh repo create newrrow-bot --private
```

- `Create a new repository on GitHub from scratch` 선택
- 나머지는 Enter

---

### 5. 나(코드 수정할 사람) 초대

GitHub 웹에서:
> 방금 만든 repo → **Settings** → **Collaborators** → **Add people**
> → **나의 GitHub 아이디** 입력 → `Write` 권한으로 초대

---

## PART 2. 내 컴퓨터에서 할 일

### 6. 초대 수락

이메일 또는 GitHub 알림에서 초대 수락

---

### 7. 코드 push

```bash
cd "/Users/dg3w04/뉴로우 회고 시스템 자동화"

git remote add origin https://github.com/상대방_아이디/newrrow-bot.git
git branch -M main
git push -u origin main
```

> 이후 코드 수정하면 `git add . && git commit -m "수정내용" && git push` 로 올리면 됨

---

## PART 3. 다시 상대방 컴퓨터에서 할 일

### 8. Node.js 설치 확인

```bash
node --version
```

없으면:
```bash
brew install node
```

---

### 9. 코드 받기

```bash
cd ~
git clone https://github.com/상대방_아이디/newrrow-bot.git
cd newrrow-bot
npm install
```

---

### 10. .env 파일 만들기

```bash
nano .env
```

내용 붙여넣기 (나한테 받은 토큰):
```
DISCORD_TOKEN=여기에_토큰
GEMINI_API_KEY=여기에_키
```

저장: `Ctrl+O` → Enter → `Ctrl+X`

---

### 11. pm2 설치 & 봇 실행

```bash
npm install -g pm2
pm2 start index.js --name bot
pm2 save
pm2 startup
```

> `pm2 startup` 실행하면 명령어 하나 출력됨 → 그거 복사해서 그대로 실행
> (컴퓨터 재부팅해도 봇 자동 켜짐)

---

### 12. 자동 업데이트 스크립트 만들기

```bash
nano ~/newrrow-bot/update.sh
```

아래 내용 붙여넣기:

```bash
#!/bin/bash
cd ~/newrrow-bot

RESULT=$(git pull)

if [ "$RESULT" != "Already up to date." ]; then
  echo "[$(date)] 변경 감지 → 재시작"
  npm install
  pm2 restart bot
else
  echo "[$(date)] 변경 없음"
fi
```

저장: `Ctrl+O` → Enter → `Ctrl+X`

실행 권한 부여:
```bash
chmod +x ~/newrrow-bot/update.sh
```

---

### 13. cron으로 1분마다 자동 실행

```bash
crontab -e
```

아래 한 줄 추가:

```
*/1 * * * * /bin/bash ~/newrrow-bot/update.sh >> ~/newrrow-bot/update.log 2>&1
```

저장 후 종료 (vim 기준: `Esc` → `:wq` → Enter)

> 변경사항 없으면 git pull이 거의 부하 없으므로 1분마다 체크해도 괜찮음

---

## 이후 사용 방법

| 상황 | 할 일 |
|------|--------|
| 코드 수정 후 반영 | 내 컴퓨터에서 `git push` |
| 상대방 컴퓨터 자동 반영 | 최대 1분 후 자동 적용 |
| 봇 상태 확인 | `pm2 status` |
| 봇 로그 확인 | `pm2 logs bot` |
| 업데이트 로그 확인 | `cat ~/newrrow-bot/update.log` |
| 즉시 업데이트 | `bash ~/newrrow-bot/update.sh` |
